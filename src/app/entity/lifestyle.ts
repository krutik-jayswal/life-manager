export class LifeStyle {

    id: number;
    date: Date;
    text: string;

    constructor(id: number, date: Date, text: string) {
        this.id = id;
        this.date = date;
        this.text = text;
    }
}